/**
 * Top-level Contracts list page — Phase 1 shell.
 *
 * The Surface+Reports worker (Phase 2c) replaces this stub with the real
 * paginated/sortable list mirroring ProposalsPage.
 */

export default function ContractsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold text-gray-900">Contracts</h1>
      <p className="mt-2 text-sm text-gray-600">
        Contract management is coming online — this surface is being built.
      </p>
    </div>
  );
}
